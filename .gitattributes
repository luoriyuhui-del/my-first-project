# 基本文本文件处理（推荐所有项目）
* text=auto
# Java 源代码文件（强制 LF 存储，检出时保持 LF，避免跨平台 IDE 格式化冲突）
*.java text eol=lf
*.xml text eol=lf
*.properties text eol=lf

# 脚本文件（强制 LF，避免执行时报错）
*.sh text eol=lf

# 二进制文件（不转换，按原样存储）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.jar binary
*.iml binary        

# 文档文件（保留 CRLF 或按需求设置）
*.md text eol=lf
*.txt text eol=lf